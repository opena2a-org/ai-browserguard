{
  "name": "@opena2a/aibrowserguard",
  "version": "0.4.2",
  "description": "AI Browser Guard - Detect, monitor, and control AI agents operating in your browser",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc --noEmit && node scripts/build.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit --strict",
    "watch": "vite build --watch",
    "smoke:export": "node scripts/smoke-export.mjs",
    "smoke:load": "node scripts/smoke-load.mjs",
    "bump": "node scripts/bump-version.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "chromedriver": "^146.0.2",
    "jsdom": "^25.0.1",
    "playwright": "^1.58.2",
    "puppeteer": "^24.37.5",
    "selenium-webdriver": "^4.41.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.21",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "private": true,
  "license": "Apache-2.0"
}
